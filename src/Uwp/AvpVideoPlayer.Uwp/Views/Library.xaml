<UserControl
    x:Class="AvpVideoPlayer.Uwp.Views.Library"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AvpVideoPlayer.Uwp.Views"
    xmlns:viewmodels="using:AvpVideoPlayer.Uwp.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    d:DataContext="{d:DesignInstance viewmodels:LibraryViewModel, IsDesignTimeCreatable=False}">

    <Grid VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="5*"   />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Height="40">
            <local:FolderDropDown DataContext="{Binding FolderDropDownViewModel}" Margin="5 1 0 4" />
            <local:SearchBox Width="180" HorizontalAlignment="Right" Height="30"  
                             Margin="02 2 0 0" DataContext="{Binding SearchBoxViewModel}"></local:SearchBox>
        </StackPanel>
        <local:FileList Grid.Row="1" DataContext="{Binding FileListViewModel}"></local:FileList>
    </Grid>
</UserControl>
