<UserControl
    x:Class="AvpVideoPlayer.Uwp.Views.VideoThumbnail"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AvpVideoPlayer.Uwp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:viewmodels="using:AvpVideoPlayer.Uwp.ViewModels"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    d:DataContext="{d:DesignInstance viewmodels:VideoFileViewModel, IsDesignTimeCreatable=False}">

    <StackPanel Orientation="Horizontal">
        <Image Width="24" Source="{Binding FileType, Converter={StaticResource FileTypeToIconConverter}}" Margin="0 -3" />
        <TextBlock Text="{Binding Name}" Foreground="{StaticResource LightForegroundBrush}" Margin="4 3" />
    </StackPanel>
</UserControl>
