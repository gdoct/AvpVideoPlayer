<Page
    x:Class="AvpVideoPlayer.Uwp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AvpVideoPlayer.Uwp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:views="using:AvpVideoPlayer.Uwp.Views" xmlns:viewmodels="using:AvpVideoPlayer.Uwp.ViewModels"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    d:DataContext="{d:DesignInstance viewmodels:MainWindowViewModel, IsDesignTimeCreatable=False}">
   
    <Grid>
        <NavigationView >
            <NavigationView.MenuItems>
                <NavigationViewItem Icon="Video" Tag="Home_Page" >
                </NavigationViewItem>
           </NavigationView.MenuItems>
            <NavigationView.PaneCustomContent>
                <views:Library x:Name="Library1" DataContext="{Binding LibraryViewModel}" VerticalAlignment="Stretch"></views:Library>
            </NavigationView.PaneCustomContent>

        </NavigationView>
        <!--<views:Library x:Name="Library" DataContext="{Binding LibraryViewModel}"></views:Library>-->
        <views:VideoPlayer  x:Name="MediaPlayer" DataContext="{Binding VideoPlayerViewModel}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"></views:VideoPlayer>
    </Grid>
</Page>
